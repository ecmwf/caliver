# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
os: linux

addons:
  apt:
    sources:
      - sourceline: 'ppa:ubuntugis/ubuntugis-unstable'
    packages:
      - libproj-dev
      - proj-bin
      - libgdal-dev
      - gdal-bin
      - libnetcdf-dev
      - netcdf-bin
      - libharfbuzz-dev
      - libfribidi-dev

r_packages:
  - lintr
  - covr
  - pkgdown

r_github_packages:
  - ecmwf/caliver

after_success:
  - Rscript -e 'covr::codecov(line_exclusions = list("R/zzz.R", "R/experimental.R, "R/defunct.R"))'
  - Rscript -e 'pkgdown::build_site()'

deploy:
  provider: pages
  cleanup: true
  github-token: $GITHUB_PAT
  keep-history: true
  on:
    branch: master
